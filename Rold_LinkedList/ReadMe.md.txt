The following files comprise an Iterable, Doubly-Linked, Circuluar LinkedList.

The LinkedList can store any and all objects.
Use of ListNodeIterator with a non-circular list will require prior modification.